{
  "name": "@freik/ts-cpp-tooling",
  "version": "0.3.0",
  "description": "A simple IDL for use with CrowCpp (C++) and TypeScript",
  "module": "build/index.js",
  "type": "module",
  "files": [
    "build"
  ],
  "types": "build/src/index.d.ts",
  "bin": {
    "crow-idl": "build/src/crow-idl.js",
    "ts-class-graph": "build/src/ts-class-graph.js"
  },
  "scripts": {
    "format": "tools format bun",
    "clean": "rm -rf build && git restore build/NOTE.md",
    "dcfg": "cmake --preset debug",
    "rcfg": "cmake --preset release",
    "dcpp": "cmake --build build/Debug",
    "rcpp": "cmake --build build/Release",
    "typechk": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "bld": "tsc && bun build --outdir build ./index.ts ./src/crow-idl.ts ./src/ts-class-graph.ts --minify --external typescript --external prettier --external @freik/typechk --external ts-morph",
    "lint": "eslint --ext .ts src --fix",
    "justtest": "bun test",
    "watch": "bun test --watch",
    "test": "bun run typechk && bun run justtest",
    "prepublishOnly": "bun run bld",
    "*": "Hooks for Husky",
    "prepare": "husky",
    "hook-commit": "bun run format",
    "hook-push": "bun run test"
  },
  "dependencies": {
    "prettier-plugin-organize-imports": "^4.3.0",
    "ts-morph": "^27.0.2"
  },
  "peerDependencies": {
    "@freik/typechk": "^0.9.1",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@freik/typechk": "^0.9.1",
    "@freik/workspace": "^0.6.5",
    "@types/bun": "^1.3.9",
    "clang-format": "^1.8.0",
    "eslint": "^10.0.1",
    "globals": "^17.3.0",
    "husky": "^9.1.7",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.56.0"
  }
}
